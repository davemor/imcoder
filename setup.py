from setuptools import find_packages, setup

setup(
    name="imcoder",
    packages=find_packages(),
    version="0.1.0",
    description="Have fun encoding images with neural networks.",
    author="David Morrison",
)
